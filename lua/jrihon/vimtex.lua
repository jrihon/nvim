
--[[ ------------------------------------------------------------------
                        VIMTEX CONFIGURATION
  ------------------------------------------------------------------ ]]
-- Only this makes VimTex compile. Found on ':help VimTex'
--
--$ sudo apt install latexmk
